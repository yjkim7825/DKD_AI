# P25 DKD 바이오마커 파이프라인 (Python 이식본) 의존성
# 로컬 venv 에서 설치:  pip install -r requirements.txt
numpy>=1.24
pandas>=2.0
scipy>=1.10            # limma eBayes (digamma/polygamma/t) 에 필요
scikit-learn>=1.3      # LASSO(LogisticRegressionCV), SVM-RFE(RFECV), ROC
matplotlib>=3.7        # 화산도/ROC 그림 (선택)

# --- 확장(옵션) ---
# gseapy>=1.1          # GSEA.R 대응 (KEGG pathway enrichment)
# statsmodels>=0.14    # 추가 통계
